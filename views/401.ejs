<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/_bootstrapCSS') %>
    <title>401</title>
  </head>

  <body>
    <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">401 - You don't have permission to see that!</a></h5>
      </div>
    </div>
    <div class="card text-center">
      <div class="card-body">
        <form method="GET" action="/urls"><button class="btn btn-primary">Return to Tinyapp</button></form>
      </div>
      <div class="card-footer text-muted"></div>
    </div>
    <%- include('partials/_bootstrapJS') %>
  </body>
</html>